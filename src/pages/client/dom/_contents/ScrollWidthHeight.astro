---
import Card from '@/components/common/Card.astro';
import Code from '@/components/common/Code.astro';

export const contentOrder = 9;
export const id = 'scrollWidthHeight';
const code = ``;
---

<Card id={id}>
  <h2>element.scrollWidth / Heightについて</h2>
  <p>
    画面幅からはみ出た部分も幅・高さを取得する。スクロール可能な内容全体のサイズ
  </p>
  <Code code={code} />
  <div class='box'>
    <div class='box__content'>BOX</div>
  </div>
  <button type='button'>取得する</button>
  <output></output>
</Card>

<style>
  .box {
    width: 100px;
    height: 100px;
    position: relative;
    overflow: scroll;
    .box__content {
      position: absolute;
      width: 900px;
      height: 200px;
      background: #ccc;
      text-align: center;
      align-content: center;
    }
  }
</style>

<script>
  const root = document.getElementById('scrollWidthHeight');
  if (root) {
    const button = root.querySelector('button');
    const output = root.querySelector('output');
    const box = root.querySelector('.box');
    if (button && output && box) {
      let unit = 'px';
      button.addEventListener('click', () => {
        output.textContent = [
          `幅: ${box.scrollWidth + unit}`,
          `高さ: ${box.scrollHeight + unit}`,
        ].join('\n');
      });
    }
  }
</script>
