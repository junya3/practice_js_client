---
import Card from '@/components/common/Card.astro';
import Code from '@/components/common/Code.astro';

export const contentOrder = 11;
export const id = 'referrer';
const code = `console.log(document.referrer);
// http://localhost:4321/client/dom`;
---

<Card id={id}>
  <h2>document.{id}について</h2>
  <p>
    リファラーの取得を行います。直前にアクセスしていたページのURLを取得し、なければ空文字を吐きます。フルパスで出力します。
  </p>
  <Code code={code} />
  <button type='button'>前ページのURLを取得</button>
  <output></output>
</Card>

<script>
  const root = document.getElementById('referrer');
  if (root) {
    const button = root.querySelector('button');
    const output = root.querySelector('output');
    if (button && output) {
      button.addEventListener('click', () => {
        output.textContent = document.referrer;
      });
    }
  }
</script>
