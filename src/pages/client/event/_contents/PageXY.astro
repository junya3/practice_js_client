---
import Card from '@/components/common/Card.astro';
import Code from '@/components/common/Code.astro';

export const contentOrder = 5;
export const id = 'pageXY';
const code = `element.addEventListener('click', (e) => {
  console.log(e.pageX, e.pageY);
});`;
---

<Card id={id}>
  <h2>e.pageX / Yについて</h2>
  <p>
    イベントが起きた座標をページを基準に取得する。e.target.scrollTop /
    Leftは要素基準。e.pageX / Yはマウス基準である。
  </p>
  <Code code={code} />
  <button type='button'>取得する</button>
  <output></output>
</Card>

<script>
  const root = document.getElementById('pageXY');
  if (root) {
    const button = root.querySelector('button');
    const output = root.querySelector('output');
    if (button && output) {
      button.addEventListener('click', (e) => {
        output.textContent = [
          `X座標: ${e.pageX}px`,
          `Y座標: ${e.pageY}px`,
        ].join('\n');
      });
    }
  }
</script>
